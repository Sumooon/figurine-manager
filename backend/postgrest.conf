# PostgREST 配置
# 数据库连接
db-uri = "postgres://dev:dev123@192.168.31.18:43637/app"
db-schema = "public"
db-anon-role = "web_anon"

# 服务器配置
server-host = "0.0.0.0"
server-port = 3000

# JWT 认证（暂时关闭，后续可开启）
# jwt-secret = "your-secret-key"

# 允许的操作
db-pre-request = "public.check_role"